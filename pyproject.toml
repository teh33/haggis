[project]
name = "haggis-ai"
version = "0.1.0"
description = "Rules engine and AI scaffold for Haggis"
requires-python = ">=3.11"
dependencies = ["torch>=2.0"]

[tool.setuptools.packages.find]
include = ["haggis*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
